services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs_app
    ports:
      - "1004:8090"
    environment:
      - NODE_ENV=production
    restart: always